
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: qre-orchestrator
  namespace: ${QRE_NS}    
spec:
  selector:
    matchLabels:
      app: qre-orchestrator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: qre-orchestrator
    spec:
      serviceAccountName: qreorch
      containers:
        - name: qre-orchestrator
          
          image: harbor.dell.com/dojo-harbor/qre/orchestrator
          env:
          - name: DB_HOST
            value: "mysql"
          - name: DB_PORT
            value: "3306"
          - name: DB_USER
            value: "qre"
          - name: DB_PASSWORD
            value: "qre"
          - name: DB_NAME
            value: "qre"
          - name: SSO_SECRET_KEY
            value: "g;pqj235hkngfava@^nffKN23"
          - name: SSO_SESSION_TYPE
            value: "filesystem"
          - name: SSO_TOKEN_URL
            value: "https://appsso.login.scfd.isus.emc.com/oauth/token"
          - name: SSO_AUTH_URL
            value: "https://appsso.login.scfd.isus.emc.com/oauth/authorize"
          - name: SSO_INFO_URL
            value: "https://appsso.login.scfd.isus.emc.com/userinfo"                                             
          ports:
          - containerPort: 8080
      # imagePullSecrets:
      #   - name: ${image_secret}

---

apiVersion: v1
kind: Service
metadata:
  name: qre-orchestrator
  namespace: ${QRE_NS}
spec:
  # loadBalancerIP: 100.80.243.207
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: qre-orchestrator
  # type: LoadBalancer

---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: qre-ingress-orchestrator
  namespace: ${QRE_NS}
  annotations:
    # use the shared ingress-nginx
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: ${QRE_NS}.oro-sandbox-small1.k8s.cec.lab.emc.com
    http:
      paths:
      - path: /
        backend:
          serviceName: qre-orchestrator
          servicePort: 8080
